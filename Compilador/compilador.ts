let canal: string = 'Gaveta';
let inscritos: number = 610234;

// canal = inscritos;
console.log(canal);

// let nome = 'Pedro'; -> dupla declaração da mesma variável no mesmo escopo;

// function soma(a, b) {
//   return a + b;
// } // -> Erro do tipo "noImplicityAny"

// function Saudar(isManha: boolean, isTarde: boolean): string {
//   let saudacao: string;
//   let a = 1;
//   isManha ? (saudacao = 'Bom dia') : (saudacao = '');
//   return saudacao;
// } // -> Erro do tipo "StrictNullCheck", "noUnusedParameter", "noUnusedLocal"
